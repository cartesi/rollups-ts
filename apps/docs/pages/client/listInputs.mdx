# listInputs

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/client";

const publicClientL2 = createCartesiPublicClient({
    transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listInputs({
    application: "0x...",
    limit: 10,
    offset: 0,
    // epochIndex, sender, from, to (optional)
});
```

## Parameters

```ts twoslash
import type { ListInputsParams } from "@cartesi/client";
// {
//   application: Address | string;
//   epochIndex?: bigint;
//   sender?: Address;
//   transactionHash?: Hash;
//   from?: bigint;
//   to?: bigint;
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListInputsReturnType, Input, Pagination } from "@cartesi/client";
// ListInputsReturnType = {
//   data: Input[];
//   pagination: Pagination;
// }
```
