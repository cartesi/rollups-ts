---
"@cartesi/client": patch
"@cartesi/codec": patch
"@cartesi/machine": patch
"@cartesi/react": patch
"@cartesi/rollup": patch
"@cartesi/rpc": patch
"@cartesi/wagmi-plugin": patch
---

ship the Apache-2.0 licence with the package: every tarball now carries `LICENSE` and `NOTICE`, and the packages that declared no `license` field (`client`, `codec`, `rpc`, `react`, `wagmi-plugin`) declare `Apache-2.0`
