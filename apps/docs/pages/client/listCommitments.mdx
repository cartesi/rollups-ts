# listCommitments

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/client";

const publicClientL2 = createCartesiPublicClient({
  transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listCommitments({
  application: "0x...",
  limit: 10,
  offset: 0,
  // status (optional)
});
```

## Parameters

```ts twoslash
import type { ListCommitmentsParams } from "@cartesi/client";
// {
//   application: Address | string;
//   epochIndex?: bigint;
//   tournamentAddress?: Address;
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListCommitmentsReturnType, Commitment, Pagination } from "@cartesi/client";
// ListCommitmentsReturnType = {
//   data: Commitment[];
//   pagination: Pagination;
// }
```
