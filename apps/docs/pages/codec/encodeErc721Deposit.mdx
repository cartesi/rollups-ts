# encodeErc721Deposit

Encodes an ERC-721 deposit payload as the ERC-721 portal does. This is the inverse of [decodeErc721Deposit](/codec/decodeErc721Deposit), useful for testing.

## Usage

```ts twoslash
import { encodeErc721Deposit } from "@cartesi/codec";

const payload = encodeErc721Deposit({
  token: "0x67742ff5b2b762503ff0a92738c6fc2ea4a4d182",
  sender: "0x92cc14432c1f82622493abd64d99ea8a3000a7c7",
  tokenId: 42n,
  baseLayerData: "0x",
  execLayerData: "0xdeadbeef",
});
```

## Parameters

An `Erc721Deposit` object:

- `token` (`Address`): token contract.
- `sender` (`Address`): token sender.
- `tokenId` (`bigint`): token identifier.
- `baseLayerData` (`Hex`): additional data to be interpreted by the base layer.
- `execLayerData` (`Hex`): additional data to be interpreted by the execution layer.

## Return Type

`Hex`: packed-encoded deposit payload, with layout `token (20 bytes) ‖ sender (20 bytes) ‖ tokenId (32 bytes) ‖ abi.encode(baseLayerData, execLayerData)`.
