# listReports

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/viem";

const publicClientL2 = createCartesiPublicClient({
  transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listReports({
  application: "0x...",
  limit: 10,
  offset: 0,
  // epochIndex, inputIndex (optional)
});
```

## Parameters

```ts twoslash
import type { ListReportsParams } from "@cartesi/viem";
// {
//   application: Address | string;
//   epochIndex?: bigint;
//   inputIndex?: bigint;
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListReportsReturnType, Report, Pagination } from "@cartesi/viem";
// ListReportsReturnType = {
//   data: Report[];
//   pagination: Pagination;
// }
```
