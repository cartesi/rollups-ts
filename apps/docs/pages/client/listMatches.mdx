# listMatches

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/client";

const publicClientL2 = createCartesiPublicClient({
  transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listMatches({
  application: "0x...",
  limit: 10,
  offset: 0,
  // epochIndex, tournamentAddress, matchIndex (optional)
});
```

## Parameters

```ts twoslash
import type { ListMatchesParams } from "@cartesi/client";
// {
//   application: Address | string;
//   epochIndex?: bigint;
//   tournamentAddress?: Address;
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListMatchesReturnType, Match, Pagination } from "@cartesi/client";
// ListMatchesReturnType = {
//   data: Match[];
//   pagination: Pagination;
// }
```
