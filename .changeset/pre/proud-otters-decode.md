---
"@cartesi/codec": major
---

first release of the codec package, with isomorphic utility functions to decode (and encode, for testing) data compliant with the rollups contracts `Inputs` and `Outputs` interfaces, and portal deposit payloads packed-encoded by the `InputEncoding` library
