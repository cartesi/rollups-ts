---
"@cartesi/wagmi-plugin": minor
---

bump the default rollups-contracts release from v3.0.0-alpha.6 to v3.0.0-alpha.7, updating `DEFAULT_VERSION`, the tarball URLs (release assets are now named `cartesi-rollups-contracts-<version>-*.tar.gz`) and their SHA-256 hashes
