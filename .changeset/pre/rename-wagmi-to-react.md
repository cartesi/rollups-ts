---
"@cartesi/react": minor
---

Rename the package from `@cartesi/wagmi` to `@cartesi/react`. Update imports from `@cartesi/wagmi` to `@cartesi/react`; the exported hooks, providers, and query-key helpers are unchanged. The `@cartesi/wagmi-plugin` package (the `@wagmi/cli` plugin) is unaffected and keeps its name.
