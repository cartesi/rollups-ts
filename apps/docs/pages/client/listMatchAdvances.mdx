# listMatchAdvances

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/client";

const publicClientL2 = createCartesiPublicClient({
  transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listMatchAdvances({
  application: "0x...",
  epochIndex: 1n,
  tournamentAddress: "0x...",
  idHash: "0x...",
  limit: 10,
  offset: 0,
});
```

## Parameters

```ts twoslash
import type { ListMatchAdvancesParams } from "@cartesi/client";
// {
//   application: Address | string;
//   epochIndex: bigint;
//   tournamentAddress?: Address;
//   idHash: Hash;
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListMatchAdvancesReturnType, MatchAdvanced, Pagination } from "@cartesi/client";
// ListMatchAdvancesReturnType = {
//   data: MatchAdvanced[];
//   pagination: Pagination;
// }
```
