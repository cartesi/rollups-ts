# listApplications

## Usage

```ts twoslash
import { http } from "viem";
import { createCartesiPublicClient } from "@cartesi/viem";

const publicClientL2 = createCartesiPublicClient({
  transport: http("http://127.0.0.1:6751/rpc"),
});

const { data, pagination } = await publicClientL2.listApplications({
  limit: 10,
  offset: 0,
});
```

## Parameters

```ts twoslash
import type { ListApplicationsParams } from "@cartesi/viem";
// {
//   limit?: number;
//   offset?: number;
//   descending?: boolean;
// }
```

## Return Type

```ts twoslash
import type { ListApplicationsReturnType, Application, Pagination } from "@cartesi/viem";
// ListApplicationsReturnType = {
//   data: Application[];
//   pagination: Pagination;
// }
```
